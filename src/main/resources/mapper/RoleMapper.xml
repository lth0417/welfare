<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.com.sise.welfare.mapper.RoleMapper">

    <select id="selectRole" parameterType="cn.com.sise.welfare.model.RoleSearchModel"
            resultType="cn.com.sise.welfare.entity.RoleEntity">
        select * from tb_role
        <where>
            <if test=" r.role != null and r.role != '' ">
                and role = #{r.role}
            </if>
        </where>
    </select>

    <insert id="addRoleInfo" parameterType="cn.com.sise.welfare.model.RoleSearchModel">
        insert into tb_role (role) values (${role})
    </insert>

    <update id="updateRoleInfo" parameterType="cn.com.sise.welfare.model.RoleSearchModel">
        update tb_role set role = ${role} where id = #{id}
    </update>

</mapper>
